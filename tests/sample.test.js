/* eslint-disable no-undef */

describe('Sample Test Suite', () => {
    it('should always return OK', () => {
      // TODO, this just verifies that the test suite is working.
      const result = 'OK';
      expect(result).toBe('OK');
    });
  });
  